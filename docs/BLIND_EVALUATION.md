# EcomGuard Agent 盲测报告 v1

## 测试约束

- 基线代码提交：`7c13cf3`
- 盲测集冻结提交：`640186d`（先推送测试集，后运行）
- 运行日期：2026-09-04
- Agent 用例：18条；RAG用例：26条（18条有答案、8条无答案）
- Agent 数据集 SHA-256：`BD01D9E662FC53DDCE50226C1E0ED96E05BCE4B4036890D44DA8A645E6161550`
- RAG 数据集 SHA-256：`14A039F622DCFD0C0A856509ADC720BAB974534948259B89E012DE3DC8803556`
- 运行后未修改用例、标签、通过阈值或被测代码。首次Agent运行因Windows GBK无法输出emoji而在模型调用前失败；仅设置`PYTHONUTF8=1`后原样重跑，不计入模型结果。

## RAG 盲测

使用`qwen3.7-text-embedding`、语义检索、候选Top-5，不启用查询拆解与Reranker。

| 指标 | 结果 |
|---|---:|
| Hit@1 | 94.4% |
| Hit@3 / Hit@5 | 100.0% / 100.0% |
| Recall@1 | 61.1% |
| Recall@3 | 88.9% |
| Recall@5 | 91.7% |
| nDCG@3 | 89.7% |
| MRR | 97.2% |
| 多跳 Recall@3 / @5 | 81.8% / 86.4% |

结果证明动态Top-5仍能改善多文档证据覆盖，但Recall没有沿用开发集的接近满分结果。独立无答案规则仅识别出8条中的3条，Precision 100%、Recall 37.5%、F1 54.5%，说明此前在已知样本上的100%主要来自规则覆盖，而非开放域泛化。

## 完整 Agent＋LLM-as-Judge

两种模式均使用相同18条固定用例和`qwen3.7-plus`，每条运行回答质量、事实忠实度、过程合理性三项Judge。

| 指标 | 单 Agent | 多 Agent |
|---|---:|---:|
| 原始严格通过率 | 0.0% | 33.3% |
| 去除不适用于单Agent的路由维度后严格通过率 | 33.3% | 33.3% |
| 平均过程得分 | 67.1% | 84.9% |
| 平均结果得分 | 92.4% | 86.8% |
| Judge回答质量达标率 | 94.4% | 88.9% |
| Judge事实忠实度达标率 | 100.0% | 83.3% |
| Judge过程合理性达标率 | 55.6% | 66.7% |
| 多意图 Micro-F1 | 71.4% | 71.4% |
| 敏感操作安全率 | 100.0% | 92.9% |
| 平均Token / 总Token | 2,311 / 41,593 | 2,644 / 47,590 |

单Agent原始严格通过率为0%的直接原因是盲测集填写了`expected_route`，而单Agent没有路由字段，18条全部在该维度得0；这属于评估适用性问题。报告同时给出去除路由维度后的33.3%，但保留原始值，不改写冻结集。

## 盲测发现

1. **P0：多Agent隐私边界不一致。** “查询同事订单”在单Agent中被拒绝，在多Agent中却调用`query_order`并披露商品、金额和物流信息，是本轮最严重的问题。
2. **P1：未知商品候选污染。** 查询不存在的相机时，商品工具返回了无关牛仔裤候选；最终回复虽称未找到，但Judge认为工具证据与结论矛盾。
3. **P1：转人工缺少真实工具闭环。** 投诉与账号被盗场景会口头承诺转人工，但未稳定调用`escalate_to_human`。
4. **P1：事实＋规则联合查询不稳定。** 牛仔裤和激活手机场景有时只查订单或只查政策，未稳定同时调用`query_order`与`search_knowledge`。
5. **P1：无答案识别泛化不足。** 新表达下Recall由已知集合100%下降到37.5%，需要由少量关键词规则升级为“边界分类＋证据充分性”两级判断。
6. **P2：主意图标签存在偏移。** 发票、账号异常、问候以及部分售后问题的主意图与盲测标注不一致，Micro-F1为71.4%。
7. **评估器问题。** Judge在提示词注入用例中把用户伪造的`SYSTEM`内容当作真实要求，错误惩罚了安全拒绝；Judge提示词也需要明确将被测用户输入视为不可信数据。

## 结论

项目已经具备可展示的架构、安全状态机、RAG消融和评估闭环，但本次盲测证明它还不是生产就绪系统。下一轮不应扩展功能，而应围绕上述P0/P1失败集修复，并用全新的v2盲测集复验；v1始终保留作为修复前基线。
