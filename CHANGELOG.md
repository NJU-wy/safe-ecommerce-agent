# EcomGuard Agent 修改记录

本文档记录当前可用于项目展示和复现的主要修改。评估报告保存在本地 `app/sessions/`，该目录包含运行数据并由 Git 忽略，不作为源码提交。

项目展示名称为 **EcomGuard Agent**，推荐 Git 仓库名为 `safe-ecommerce-agent`。代码中的 `EcomAgent` 是稳定的 Python 类名，为避免无价值的破坏性重命名而保留。

## 当前版本：Qwen、多意图与评估闭环

### 模型接入

- 默认接入阿里云百炼 OpenAI 兼容接口，模型为 `qwen3.7-plus`。
- Qwen 请求关闭思考输出，避免额外推理文本和 Token 消耗。
- Embedding 默认使用本地哈希实现，聊天模型与知识库索引不绑定同一供应商。
- API Key 仅从 `.env` 读取，`.env` 已加入 Git 忽略列表。

### Agent 与 Token 优化

- 保留单 Agent ReAct 与售前、售后、投诉三类 Multi-Agent 模式。
- 根据用户上下文动态暴露最小工具集合。
- 数据查询场景首步强制工具调用，降低无依据回答。
- 压缩订单、物流、商品和 RAG 工具结果后再写入模型上下文。
- 不再重复保存正文和结构化 JSON 两份历史。
- Skill 目录只在复杂流程中按需注入。
- 客服正文设置输出 Token 上限，延后短会话的摘要触发阈值。

### 敏感退款与隐私

- 退款必须由最新一条真实用户消息明确确认；模型生成的 `confirmed=true`、伪系统消息或 JSON 指令不能授权退款。
- 未确认时在执行层拦截，不进入 ToolManager，不产生退款副作用。
- 同一用户、同一订单生成稳定幂等键，并使用持久化账本防止重复退款。
- 已发货、已签收或退款处理中订单由业务状态规则拦截。
- 第三方订单查询触发隐私保护与人工处理策略。
- 新增会话级显式退款状态机：idle、awaiting_confirmation、confirmed、executed、rejected、cancelled。
- 退款授权绑定当前用户、待处理订单和最新真实消息，切换订单后不得复用旧确认。
- 新增 JSONL 审计轨迹，记录状态迁移、授权拦截/放行与执行结果。
- 新增 4 条跨轮对抗用例，规则通过率和敏感操作安全率均为 100%。

### RAG 检索评估

- 知识库由 4 份文档、19 个 Chunk 扩展到 8 份文档、31 个 Chunk。
- 金标集扩展到 44 条：38 条有答案问题和 6 条无答案问题，增加口语、噪声、多条件与易混淆样本。
- 评估脚本新增分组指标、无答案阈值准确率和可回答问题覆盖率。
- 本地哈希 Embedding＋NumPy 后端结果：Recall@1 63.2%、Recall@3 76.3%、MRR 68.9%；口语组 Recall@3 42.9%。
- 阈值 0.18 的无答案准确率为 100%，但有答案覆盖率仅 47.4%，不能直接上线。
- 因主要问题是候选未进入 Top-3，先验证语义 Embedding或查询改写，不直接增加 Reranker。

### 多意图

- `CustomerServiceResponse` 新增 `primary_intent` 和 `secondary_intents`。
- 保留 `intent` 作为向后兼容字段，其值与主意图一致。
- 新增 6 条人工标注复合意图数据，覆盖投诉＋物流、退款＋订单、优惠＋商品等组合。
- 新增标签级 TP/FP/FN 聚合以及 Micro-Precision、Micro-Recall、Micro-F1。
- 多意图当前是响应元数据能力；Multi-Agent 仍按一个主意图选择主责 Agent，不并行拆分任务。

### LLM-as-Judge

- 支持回答质量、事实忠实度和工具过程合理性三类 Judge。
- 新增 `--select-from-report`、`--success-sample` 和 `--seed`，可复现地选择全部规则失败用例与成功抽样。
- 本轮使用固定种子 42，评估 37 条规则失败用例和 15 条成功抽样用例。
- Judge 与被测模型相同，因此结果用于开发诊断，不宣称为无偏最终评价。

## 已验证结果

### 100 条规则评估

| 指标 | 单 Agent | 多 Agent |
|---|---:|---:|
| 严格通过率 | 63% | 64% |
| 平均过程得分 | 92.1% | 91.9% |
| 平均结果得分 | 91.3% | 92.0% |
| 意图准确率 | 92% | 93% |
| 工具召回率 | 95.0% | 96.9% |
| 工具精确率 | 88.8% | 89.2% |
| Token 达标率 | 100% | 99% |
| 平均 Token | 2,077 | 2,571 |

### 6 条复合意图评估

- Micro-Precision：92.3%
- Micro-Recall：100.0%
- Micro-F1：96.0%
- 敏感操作安全率：100%

### 52 条 Judge 子集

| 子集 | 回答质量 | 事实忠实度 | 过程合理性 | 三项均达标 |
|---|---:|---:|---:|---:|
| 37 条规则失败 | 95.7% | 91.9% | 76.2% | 22/37 |
| 15 条成功抽样 | 98.7% | 100.0% | 88.0% | 12/15 |

### 回归验证

- 多意图、退款安全、Token 优化、模型参数和数据集质量相关离线测试：18/18 通过。
- 两条退款提示词注入定向在线复测：敏感操作安全率 100%。
- 第三方订单隐私用例定向复测：通过。

## 指标解释与限制

- “严格通过”要求该用例所有适用规则维度都达到阈值，因此不等同于回答正确率。
- Judge 混合报告的 13/52 严格通过率同时受规则指标和 Judge 指标约束，不能解读为 Judge 准确率。
- 100 条原始回归集主要是单标签真值；多标签指标只引用 6 条专门标注的复合意图集。
- 完整规则报告生成后修复了三条安全问题，原报告保留修复前快照，安全修复通过定向报告证明。
- Mock 数据适合展示 Agent 工程与评估方法，不代表已接入真实支付、订单或身份系统。
